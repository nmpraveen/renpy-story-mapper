# M15 - MsDay1 Narrative Map correction

Status: Blocked

Blocker: Track A exact head `aa570f3ea7e6cba200cb2585f2f97386128cb07a` and Track B exact
head `6702e933dba82d19da8ea59ae246020eaebc9e80` each retain two independently reproduced P1
findings after the contract's single bounded correction and rereview. Neither track is integrated.
Renewed user authorization is required for one further narrowly bounded correction and independent
rereview per track before integration can continue.

Scope authority: `docs/MASTER_PLAN.md`; the user-approved
`docs/handoffs/2026-07-20-msday1-narrative-map/IMPLEMENTATION_PLAN.md`; and the user's explicit
2026-07-20 approval to proceed

Baseline: `a447a4eefbd7c093bdb2767e62a393805af068ac`

Integration branch: `codex/m15-msday1-narrative-map`

## Done condition

M15 is PR-ready when a human can open the exact private `MsDay1` input and immediately understand
its chronological Prologue and Day 1 story, meaningful event boundaries, four ordered temporary
choices, exact arms and proven rejoins in a readable Story Map whose connectivity comes only from
current M10 authority; every visible element reaches exact Detail/Evidence; the old route-solver
and legacy AI-map controls are absent from the normal journey without deleting their compatibility
backends or stored results; provider-free, browser, Windows, package, immutability, and independent
review gates pass with no unresolved P0-P2 correctness, security, or narrative-quality finding;
durable completion evidence and the native infographic exist; and one M15 pull request is open,
genuinely ready, and left unmerged for user approval.

## Objective

Replace the current micro-scene presentation with a readable Day 1 narrative graph whose visible
nodes are meaningful story events and whose lines, choices, gates, effects, merges, terminals, and
evidence remain deterministic.

## Deliverables

- Versioned Narrative Corridor, Boundary Decision, Narrative Event, and Narrative Map contracts.
- Deterministic ordered-corridor construction over current M10 authority and M11 story atoms.
- Evidence-bounded AI boundary-decision and final-event summary workflows with independent durable
  cache, validation, repair, cancellation, resume, and provider-free fallback.
- Deterministic contiguous event assembly and quotient topology with exact M10/M11 provenance.
- A default browser Story Map with chronological event clusters and nested temporary choices.
- Retirement of visible M12 route-solving and legacy M07/M08 AI organization controls while
  preserving backend, persistence, stored-result, old-project, and M13-citation compatibility.
- Synthetic, exact private Day 1, persistence, fault, API, UI, real-browser, package, and safety
  acceptance coverage plus durable evidence, reviews, screenshots, text export, and infographic.

## Acceptance criteria

1. Narrative Corridor, Boundary Decision, Narrative Event, and Narrative Map records have stable
   versioned identities, exact current-authority bindings, normalized hashes, and validated
   provenance.
2. Deterministic corridors preserve source/control order per lane and call occurrence, attach
   technical coverage without promotion, cut at authoritative hard boundaries, and preserve
   temporary split/rejoin ownership.
3. AI receives only adjacent soft-boundary decisions and, after membership freezes, independent
   event-summary jobs. It cannot create membership, edges, gates, effects, choice order, rejoins,
   filesystem/web/tool authority, or cross a hard boundary.
4. Python assembles complete, contiguous, non-overlapping event membership. Duplicate, missing,
   crossing, out-of-order, cross-lane, cross-occurrence, or cross-hard-boundary membership fails
   closed; unavailable or uncertain AI keeps a conservative visible boundary.
5. Narrative Map edges, choices, arms, rejoins, gates, effects, terminals, and unresolved records
   derive only from current M10 relationships and deterministic facts, with every visible item
   retaining exact M10/M11 evidence handles.
6. The exact input SHA-256 remains
   `14aa44ed95dec5402dfb02a1c4e01e63b3f3e329cf04fec37b04edebb5d588a6`; line 52 is the visible
   Day 1 anchor; menus remain anchored at lines 143, 191, 623, and 674; and line 793 remains only
   the final rejoin/scope sentinel with no later Day 2 content.
7. The accepted map contains a separate collapsible Prologue, one coherent Terrance disciplinary
   cluster with two ordered temporary choices and proven rejoins, then separate Janet, family
   dinner, and Faye clusters; Faye owns its nested choices and final line-793 rejoin.
8. The initial viewport begins with meaningful story. Developer/age/patch/notification setup,
   `Start`, `Clean`, module endings, raw image identifiers, and routine technical commands are not
   equal-weight story cards.
9. Every event, beat, choice, arm, rejoin, requirement, effect, terminal, and unresolved element
   can open exact qualified reconstructed or physical Detail/Evidence without adding a third
   semantic level.
10. The normal website has no visible `Reach this scene`, `How do I reach this?`, `routePanel`,
    `solveRoute`, old `AI Story Map`, or legacy organization controls, and makes zero M12
    solve/destination requests during open, navigation, or selection.
11. M12 backend/persistence/stored results/API readers and M13 `navigation.mode = "m12_result"`
    citations remain compatible; existing projects without a Narrative Map open through a clearly
    labelled deterministic/legacy fallback and do not crash.
12. Boundary and summary jobs persist independently; exact reopen/resume retries only missing or
    failed jobs; invalid items fail independently; event-summary failure preserves deterministic
    membership and fallback; exact cache replay makes zero provider calls.
13. Opening, rendering, navigation, search, selection, and evidence traversal make zero provider
    calls. No private story text is transmitted without a fresh exact manifest and separate
    explicit user consent; live Day 1 acceptance is optional and full MsDenvers remains deferred.
14. Pan, zoom, fit, search, selection preservation, keyboard traversal, technical/unresolved
    access, and readable temporary-choice geometry work at 100% and 200% without overflow,
    overlapping cards, severed connectors, arbitrary story slices, or invented lines.
15. Existing M10/M11/M12/M13 authority and stored-result bytes remain unchanged except for an
    intentionally versioned new Narrative Map payload; no migration deletes legacy narrative,
    organization, route, correction, claim, or evidence records.
16. The private source and original archive remain byte-, size-, and timestamp-identical after
    access; no Ren'Py, creator Python, screen, game code, or runtime tracing executes; private
    source and story text never enter Git.
17. Focused failing-first, synthetic, exact private Day 1, persistence/fault, API/UI, JavaScript,
    real-Chrome, Windows CPython 3.12 pytest, Ruff, strict mypy, `pip check`, asset-manifest,
    wheel/install/import, notice, and whitespace checks pass as applicable.
18. Track A, Track B, and Track C each receive an independent exact-head review and bounded
    correction loop; the integrated candidate receives a final read-only cross-track review with
    no unresolved P0-P2 correctness, security, or narrative-quality finding.
19. Completion evidence includes 100%/200% Day 1 screenshots, a visible-order text export,
    representative Terrance/Janet evidence traversal, provider-free and any authorized live/replay
    metrics, immutability evidence, known limitations, explicit full-MsDenvers non-validation, the
    native `INFOGRAPHIC.png`, and one ready but unmerged M15 pull request.

## Required evidence

| Criterion | Evidence required | Result / durable location |
|---|---|---|
| 1-5 | Contract/schema tests, corridor/event/quotient fixtures, provenance and fail-closed tests | Pending |
| 6-8 | Exact private Day 1 hashes, anchor assertions, golden-shape text export, screenshots | Pending |
| 9-11 | API/UI tests, real-browser traversal, zero-M12-request counter, legacy compatibility tests | Pending |
| 12-13 | Fake-provider fault/reopen/cache matrix, zero-call counters, consent manifest if separately approved | Pending |
| 14 | Real Chrome at 100% and 200%, keyboard/layout diagnostics and screenshots | Pending |
| 15-16 | Before/after payload, source, archive, size, timestamp, and execution-safety evidence | Pending |
| 17 | Exact commands, exit codes, counts, elapsed times, build/install/import artifacts | Pending |
| 18 | Track and final exact-head review reports with finding disposition | Pending |
| 19 | `COMPLETION_REPORT.md`, text export, screenshots, `INFOGRAPHIC.png`, PR URL/state | Pending |

## Exclusions

- No full-game MsDenvers provider workflow or claim that full MsDenvers is validated.
- No normal-journey route-to-target solving; M12 is deferred/hidden, not deleted or marked stale.
- No M14 dynamic adapters, runtime tracing, Ren'Py/game execution, or creator Python execution.
- No installer, public distribution, hosted service, media understanding, Q&A/chat,
  playthrough automation, game editing, or patching.
- No private `MsDay1` source, dialogue, walkthrough text, or original archive in Git.
- No provider call without a fresh exact manifest and separate explicit user consent.

## Handoff rules

- Provide the exact commit, branch/worktree, base, changed files, validation commands and results,
  assumptions, known defects, likely conflicts, and remaining acceptance work.
- Track A owns deterministic corridors, event assembly, quotient topology, and provenance only.
- Track B owns boundary/summary prompts, projection, durable jobs, validation, repair, cancellation,
  and resume only; it cannot create edges or mutate membership outside the approved assembler.
- Track C owns the Narrative Map API/browser, geometry, evidence navigation, visible legacy-surface
  retirement, compatibility guards, packaged assets, and real-browser coverage only.
- Keep status at `Integration` until worker changes are integrated and reviewed.
- Keep the native Codex goal active through integration, verification, review, evidence, and PR
  preparation. Complete it only at genuine `PR ready`.
