label start:
    menu:
        "Nested route":
            if inner_gate:
                "Inner if line"
            else:
                "Inner else line"
        "Other route":
            "Other line"
    return
