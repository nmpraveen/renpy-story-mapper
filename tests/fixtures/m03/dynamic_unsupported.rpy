label dynamic_unsupported:
    $ safe_literal = 7
    $ love = current_love + bonus
    $ points[hero_name] += amount
    $ job = choose_job()
    $ setattr(store, variable_name, 1)
    call award_points(stat_name, calculate_amount())
    if getattr(store, gate_name):
        "A computed gate must not be guessed."
    return
